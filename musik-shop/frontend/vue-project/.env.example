# Базовый путь API (относительный — через прокси Vite или тот же хост в production)
VITE_API_BASE_URL=/api

# Куда Vite проксирует /api в режиме разработки (только dev-сервер, не виден браузеру)
API_PROXY_TARGET=http://127.0.0.1:3000
